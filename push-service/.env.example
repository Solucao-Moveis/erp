# Copie para .env (que NÃO vai pro git) ou cole no painel de Environment do EasyPanel.
# A chave privada VAPID vive SÓ aqui — nunca no front nem no banco.

PORT=3000

# Segredo compartilhado com o banco (notify.push_config.secret). Invente um forte.
PUSH_SECRET=troque-por-um-segredo-forte

# Par VAPID (a PÚBLICA também está no config.js do Hub).
# A PRIVADA é secreta — NÃO commitar. Cole a sua no .env real / Environment do EasyPanel.
VAPID_PUBLIC=BPtoVpZVNWhG7bU7lU8z375Xv_tjmebZEvKfQNsyu8_hWeFEOLlQfAHztaquy7KubmDvX-FWVAvjcS_OnADxk08
VAPID_PRIVATE=cole-a-chave-privada-vapid-aqui
VAPID_SUBJECT=mailto:operacoesgeonai@gmail.com

# Supabase SMERP — a SERVICE ROLE (secreta!) pra ler as inscrições.
SUPABASE_URL=https://supabase-supabase.h5xdag.easypanel.host
SUPABASE_SERVICE_ROLE_KEY=cole-a-service-role-key-aqui
