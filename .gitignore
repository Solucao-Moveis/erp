# Documentação interna e planilhas — NÃO versionar
*.docx
*.xlsx
*.xls

# ===== CODI — NÃO versionar (contém token real e service_role key) =====
# Pasta com os PDFs de API e a collection Insomnia do CODI (token exposto)
apis codi/
# Env do coletor: CODI_TOKEN + SUPABASE_SERVICE_ROLE_KEY — NUNCA commitar
codi-coletor/.env
codi-coletor/.token
codi-coletor/.access_token

# ===== Migração (migracao/) — NÃO versionar segredos nem dumps =====
# service_role key hardcoded (acesso total ao banco, ignora RLS)
migracao/migrate-storage.js
# senha do usuário master em texto puro
migracao/master_user.sql
# senha padrão dos usuários em texto puro
migracao/set_default_password.sql
# token do export + função temporária (será apagada do Lovable)
migracao/edge-function-export/
# dumps brutos: e-mails dos usuários + signed URLs (pesados, redundantes c/ import_*.sql)
migracao/dados-fabrill/
migracao/dados-bip/
migracao/dados-compras/
migracao/dados-manutencao/
migracao/dados-pcp/
# script de download usa o token do export (mesma razão do edge-function-export/)
migracao/baixar-manutencao.ps1

# Dependências / build
node_modules/
dist/

# Serviço de push: segredos (VAPID privada, service role) — NÃO versionar
push-service/.env
# App de PC (Tauri): build pesado e schemas gerados — não versionar
src-tauri/target/
src-tauri/gen/

# Sistema
.DS_Store
Thumbs.db
